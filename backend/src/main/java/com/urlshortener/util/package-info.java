/**
 * Stateless helpers (for example Base62 encoding and IP hashing).
 *
 * <p>Restricted on purpose: this package must not become a dumping ground.
 * Anything with a domain meaning belongs in the package that owns that
 * behaviour, not here.
 */
package com.urlshortener.util;
